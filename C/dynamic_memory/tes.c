#include <stdio.h>
#include <stdlib.h>

int main () {

    int * a = (int *) malloc(20);
    char* c  = "ab";
    // printf("%d", a);
    // c++;

    int r = (int *) malloc(20);

    // *r = 16;
    // printf("%d", r);
    
    // free(c);
}